// ============================================================================
// DriveStateMachine.h (FULL INTERFACE - CLEAN + EXTENDABLE)
// ============================================================================

#pragma once

#include <stdint.h>

// ======================================================
// MAIN ENTRY POINT
// ======================================================

// ฟังก์ชันหลักของระบบขับเคลื่อน
// ต้องเรียกใน loop() ทุก cycle
void runDrive(uint32_t now);

// ======================================================
// OPTIONAL CONTROL (เผื่อใช้ในอนาคต)
// ======================================================

// reset state machine ทั้งหมด (ใช้ตอน fault / reboot / disable)
void resetDriveStateMachine();

// ======================================================
// DEBUG / STATUS (แนะนำให้มี)
// ======================================================

// อ่านสถานะปัจจุบันของ drive
uint8_t getDriveStateRaw();


